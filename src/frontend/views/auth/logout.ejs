<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FungiGT - Cerrando sesión</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #28a745;
            --secondary-color: #1d8e3a;
            --dark-color: #215c33;
            --light-color: #e6f7e9;
        }

        body {
            background-color: #f8f9fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .logout-container {
            max-width: 500px;
            padding: 2rem;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
            border-radius: 0.5rem;
            background-color: #fff;
            text-align: center;
        }

        .logout-icon {
            font-size: 4rem;
            color: var(--primary-color);
            margin-bottom: 1.5rem;
        }

        .logout-title {
            font-weight: 600;
            color: var(--dark-color);
            margin-bottom: 1rem;
        }

        .logout-text {
            color: #6c757d;
            margin-bottom: 2rem;
        }

        .logout-spinner {
            width: 3rem;
            height: 3rem;
            color: var(--primary-color);
            margin-bottom: 1.5rem;
        }

        .btn-home {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
            font-weight: 600;
            margin-top: 1rem;
        }

        .btn-home:hover {
            background-color: var(--secondary-color);
            border-color: var(--secondary-color);
        }
    </style>
</head>
<body>
    <div class="logout-container">
        <i class="fas fa-sign-out-alt logout-icon"></i>
        <h1 class="logout-title">Cerrando sesión...</h1>
        <p class="logout-text">Estamos cerrando tu sesión, por favor espera un momento.</p>
        
        <div class="spinner-border logout-spinner" role="status">
            <span class="visually-hidden">Cerrando sesión...</span>
        </div>
        
        <div id="successMessage" style="display: none;">
            <div class="alert alert-success" role="alert">
                <i class="fas fa-check-circle me-2"></i>Has cerrado sesión correctamente.
            </div>
            <a href="/" class="btn btn-primary btn-home">
                <i class="fas fa-home me-2"></i>Volver al inicio
            </a>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Eliminar tokens del almacenamiento
            localStorage.removeItem('authToken');
            localStorage.removeItem('refreshToken');
            localStorage.removeItem('user');
            sessionStorage.removeItem('authToken');
            sessionStorage.removeItem('refreshToken');
            sessionStorage.removeItem('user');
            
            // Simular tiempo de espera para mostrar el mensaje de éxito
            setTimeout(function() {
                document.querySelector('.spinner-border').style.display = 'none';
                document.querySelector('.logout-icon').className = 'fas fa-check-circle logout-icon';
                document.querySelector('.logout-title').innerText = '¡Sesión cerrada!';
                document.querySelector('.logout-text').innerText = 'Has cerrado sesión correctamente. Gracias por usar FungiGT.';
                document.getElementById('successMessage').style.display = 'block';
            }, 1500);
        });
    </script>
</body>
</html> 