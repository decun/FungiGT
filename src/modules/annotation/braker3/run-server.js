/**
 * Script para iniciar el servidor de BRAKER3
 */

const server = require('./server');
 
// Si se ejecuta este archivo directamente, se iniciará el servidor de BRAKER3
if (require.main === module) {
  console.log('Iniciando servidor de BRAKER3...');
} 